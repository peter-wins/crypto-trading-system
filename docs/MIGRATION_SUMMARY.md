# 目录结构重组总结

## 📋 变更概述

将项目从单一根目录结构重组为**前后端分离**的目录结构。

## 🔄 变更详情

### 之前的结构
```
crypto-trading-system/
├── src/              # 后端代码
├── tests/            # 测试代码
├── frontend/         # 前端代码
├── main.py           # 入口文件
├── requirements.txt  # 依赖
└── ...
```

### 现在的结构 (方案一: 独立前端目录)
```
crypto-trading-system/
├── backend/          # 后端目录 ✨
│   ├── src/         # Python源代码
│   ├── tests/       # 测试代码
│   ├── main.py      # 入口文件
│   ├── requirements.txt
│   ├── start.sh     # 启动脚本 ✨
│   └── README.md    # 后端文档 ✨
│
├── frontend/         # 前端目录
│   ├── src/
│   ├── public/
│   └── ...
│
├── config/           # 共享配置
├── docker/           # Docker配置
├── docs/             # 文档
├── logs/             # 日志
├── scripts/          # 脚本
├── .env              # 环境变量
├── docker-compose.yml # Docker编排
├── Dockerfile        # 后端Docker镜像 (已更新) ✨
└── README.md         # 项目总览 (已更新) ✨
```

## ✅ 完成的工作

### 1. 目录移动
- ✅ 创建 `backend/` 目录
- ✅ 移动 `src/` → `backend/src/`
- ✅ 移动 `tests/` → `backend/tests/`
- ✅ 移动 `main.py`, `requirements.txt`, `setup.py`, `pyproject.toml` → `backend/`
- ✅ 移动所有 `test_*.py` 文件 → `backend/`

### 2. 配置更新
- ✅ 更新 `backend/src/core/config.py`:
  - `.env` 文件路径改为 `../env` (从 backend 向上一级查找)

- ✅ 更新 `Dockerfile`:
  - requirements.txt 路径: `backend/requirements.txt`
  - 源代码路径: `backend/src/`, `backend/main.py`

### 3. 新增文件
- ✅ `backend/README.md` - 后端专用文档
- ✅ `backend/start.sh` - 启动脚本(可执行)
- ✅ 项目根目录 `README.md` - 更新为前后端分离架构说明

### 4. 测试验证
- ✅ 配置加载测试通过
- ✅ 核心模块导入测试通过
- ✅ 所有Python导入路径无需修改(因为 `setup.py` 配置了 `package_dir`)

## 🚀 使用方式

### 启动后端

**方式1: 使用启动脚本**
```bash
cd backend
./start.sh           # 启动主程序
./start.sh api       # 启动API服务器
```

**方式2: 手动启动**
```bash
cd backend
source ../venv/bin/activate
python main.py
```

### 启动前端
```bash
cd frontend
npm install
npm run dev
```

### 使用 Docker Compose (推荐)
```bash
# 从项目根目录运行
docker-compose up -d

# 后端: http://localhost:8000
# 前端: http://localhost:3000
```

## 📁 导入路径说明

**重要**: Python 导入路径**无需修改**！

由于 `backend/setup.py` 配置了：
```python
package_dir={"": "src"}
```

所以无论从哪里运行，导入路径都保持不变：
```python
from src.core.config import get_config
from src.decision.strategist import LLMStrategist
from src.execution.risk import StandardRiskManager
# ... 其他导入
```

## 🔍 配置文件路径

### .env 文件
位置: **项目根目录** (`crypto-trading-system/.env`)

从 backend 运行时，代码会自动向上一级查找 `.env` 文件。

### 其他配置
- `config/config.yaml` - 项目根目录
- `logs/` - 项目根目录
- `scripts/` - 项目根目录

## 🧪 验证结果

```bash
✅ 所有核心模块导入成功
✅ 环境: dev
✅ 分层决策启用: True
✅ 战略层间隔: 3600秒
✅ 战术层间隔: 300秒
✅ 提示词风格: balanced
```

## 📝 注意事项

1. **虚拟环境位置**: 保持在项目根目录 (`../venv` 相对于 backend)
2. **日志文件**: 仍然写入项目根目录的 `logs/`
3. **数据库**: 配置不变，仍使用项目根目录的 `.env`
4. **Docker 构建**: 需要从项目根目录运行 `docker-compose up`

## 🔗 相关文档

- [后端开发指南](./backend/README.md)
- [前端开发指南](./frontend/README.md)
- [项目总览](./README.md)
- [快速开始](./docs/QUICK_START.md)

---

**迁移完成时间**: 2025-11-09
**迁移类型**: 目录重组 (前后端分离)
**测试状态**: ✅ 通过
