# Environment
ENVIRONMENT=dev  # dev, test, prod

# Database
DATABASE_URL=postgresql://user:password@localhost:5432/crypto_trading
REDIS_URL=redis://localhost:6379/0
QDRANT_URL=http://localhost:6333


# AI Models
# AI Provider Selection: deepseek, qwen
AI_PROVIDER=qwen

DEEPSEEK_API_KEY=your_deepseek_api_key_here
DEEPSEEK_BASE_URL=https://api.deepseek.com/v1
DEEPSEEK_MODEL=deepseek-chat


# Qwen (千问) - 阿里云百炼
QWEN_API_KEY=你的千问API密钥
QWEN_BASE_URL=https://dashscope.aliyuncs.com/compatible-mode/v1
QWEN_MODEL=qwen-plus

# OpenAI (Embedding & Backup)
OPENAI_API_KEY=your_openai_api_key_here
OPENAI_EMBEDDING_MODEL=text-embedding-ada-002

# 数据源配置（拉取行情数据）
DATA_SOURCE_EXCHANGE=binance  # 可选: hyperliquid, binance, binanceusdm, okx, bybit
DATA_SOURCE_SYMBOLS=BTC/USDT,ETH/USDT  # 交易对列表（逗号分隔）
DATA_COLLECTION_INTERVAL=5  # 数据采集间隔（秒）

# Exchange APIs (Use Testnet for Development!)
# Binance
BINANCE_API_KEY=your_binance_api_key_here
BINANCE_API_SECRET=your_binance_api_secret_here
BINANCE_TESTNET=true # 若先跑正式账号，请设为 false；想跑测试网就保持 true
BINANCE_FUTURES=false # true 表示使用 USDT 永续合约，false 表示使用现货

# OKX (Optional)
OKX_API_KEY=
OKX_API_SECRET=
OKX_PASSWORD=
OKX_TESTNET=true

# System Configuration
ENABLE_TRADING=false  # Set to true only when ready for real trading
LOG_LEVEL=INFO  # DEBUG, INFO, WARNING, ERROR, CRITICAL
LOG_FILE=logs/trading_system.log
TIMEZONE=Asia/Dubai  # 系统时区: Asia/Dubai(阿布扎比/迪拜), Asia/Shanghai, UTC, America/New_York

# Risk Parameters (Default)
MAX_POSITION_SIZE=0.2  # 每一笔持仓最大占用资金为账户总资金的 20%
MAX_DAILY_LOSS=0.05  # 5% 每日最大亏损
MAX_DRAWDOWN=0.15  # 15% 最大回撤
STOP_LOSS_PERCENTAGE=5.0 # 每笔交易设置 5% 的止损线（亏损 5% 自动触发止损）
TAKE_PROFIT_PERCENTAGE=10.0 # 每笔交易设置 10% 的止盈线（盈利 10% 自动触发止盈）

# 杠杆限制配置
MAX_LEVERAGE_MAINSTREAM=50  # BTC/ETH 最大杠杆倍数
MAX_LEVERAGE_ALTCOIN=20     # 其他币种最大杠杆倍数
HIGH_LEVERAGE_WARNING=25    # 高杠杆警告阈值（超过此值会记录警告）

# === 🆕 分层决策架构配置 ===
LAYERED_DECISION_ENABLED=true
STRATEGIST_INTERVAL=3600          # 战略层运行间隔（秒），默认1小时
TRADER_INTERVAL=300              # 战术层运行间隔（秒），默认3分钟
ENABLE_NEWS=true                 # 是否启用新闻采集（较慢，可选）
CRYPTOPANIC_API_KEY=              # CryptoPanic API Key（可选）

# === 🎯 提示词风格配置 ===
# 可选值: conservative (保守 1-3), balanced (中性 4-6), aggressive (激进 7-8)
PROMPT_STYLE=balanced  # 默认中性策略
# conservative: 风险优先，严格止损，风险收益比>2.0，高信心度门槛
# balanced: 风险收益平衡，风险收益比>1.2，适度门槛
# aggressive: 机会优先，积极进取，风险收益比>1.0，低信心度门槛

# API Server
API_HOST=0.0.0.0
API_PORT=8000
API_RELOAD=true  # Auto-reload in development

# Monitoring (Optional)
PROMETHEUS_PORT=9090 # 监控端口，默认 9090
GRAFANA_PORT=3000 # 可视化监控端口，默认 3000

# Security
SECRET_KEY=your_secret_key_here_change_in_production
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30
